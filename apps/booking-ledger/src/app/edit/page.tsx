import AppWrapper from "@/components/AppWrapper"
import FormPage from "../FormPageClient"

export default function EditPage() {
  return (
    <AppWrapper>
      <FormPage mode="edit" />
    </AppWrapper>
  )
}
